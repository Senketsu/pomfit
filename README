pomfit - Pomf it !
==================

About:

Simple image and file uploader for http://pomf.se written in C using GTK.
Just alternative to bash scripts lying around.

==================
Features:
	Global hotkeys
	Notifications
==================
Install:
	Extract -> ./configure -> make ->(su/sudo) make install
